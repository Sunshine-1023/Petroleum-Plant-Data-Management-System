-- MySQL 8 初始化腳本
-- 可在 Navicat 或 mysql 命令列中執行

CREATE DATABASE IF NOT EXISTS zyxt
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

-- 如需建立專用帳號（可選，Navicat 中執行）：
-- CREATE USER 'db_user'@'localhost' IDENTIFIED BY 'your_password';
-- GRANT ALL PRIVILEGES ON zyxt.* TO 'db_user'@'localhost';
-- FLUSH PRIVILEGES;
